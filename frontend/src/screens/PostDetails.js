/* @flow */
import * as React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div``;

type Props = {};
type State = {};

class PostDetails extends React.Component<Props, State> {
  state = {};

  render() {
    return (
      <Wrapper>PostDetails</Wrapper>
    );
  }
}

export default PostDetails;
